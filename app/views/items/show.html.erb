<%= render 'shared/flash' %>
<h3>Item details</h3>
<p><%= @item.brand.name %></p>
<p><%= @item.category.name %></p>
<p><%= @item.name %></p>
<p><%= @item.notes %></p>
<p><%= @item.buffer_quantity %></p>
<%= link_to "Download Attached File", @item.file, class: "btn btn-primary", target: "_blank" if @item.file.attached? %>