<div class="row">
  <%= render 'shared/flash' %>
  <div class=" col-6 form-group">
    <%= form_tag employee_login_path do |f| %>
       <div class="field">
        <%= label_tag :email %>
        <%= text_field_tag :email %>
        </div>
        <div class="actions">
        <%= submit_tag "Login" %>
        </div>
     <% end %>
  </div>

